package academy.week01;

/**
 * Week 1 · Lesson 4 — strings and loops over characters.
 * Write code only inside the methods. Do not change method signatures.
 */
public class StringTasks {

    /**
     * Length of the string without using text.length() — count with a loop.
     * Hint: you can iterate over text.toCharArray().
     */
    public int length(String text) {
        throw new UnsupportedOperationException("TODO");
    }

    /**
     * How many times character ch appears in the string.
     * Example: "hello", 'l' → 2.
     */
    public int countChar(String text, char ch) {
        throw new UnsupportedOperationException("TODO");
    }

    /**
     * Reverse the string.
     * Example: "ab" → "ba", "" → "".
     */
    public String reverse(String text) {
        throw new UnsupportedOperationException("TODO");
    }

    /**
     * Is it a palindrome? Compare the string from the left and from the right.
     * Case matters: "AbA" → true, "Aba" → false.
     * Example: "level" → true, "java" → false.
     */
    public boolean isPalindrome(String text) {
        throw new UnsupportedOperationException("TODO");
    }

    /**
     * How many vowels are in the string. Vowels: a e i o u (and uppercase).
     * Do not count other letters or symbols.
     * Example: "Java" → 2.
     */
    public int countVowels(String text) {
        throw new UnsupportedOperationException("TODO");
    }
}
