package academy.week02;

/**
 * Week 2 · Lesson 4 (part 2). A percent discount.
 * apply: price minus percent percent. Drop the fractional part (integer division).
 * Example: price=200, percent=10 → 180.
 * If the result is less than 0, return 0.
 */
public class PercentDiscount implements Discount {

    /**
     * Store percent in a field.
     */
    public PercentDiscount(int percent) {
        throw new UnsupportedOperationException("TODO");
    }

    @Override
    public int apply(int price) {
        throw new UnsupportedOperationException("TODO");
    }
}
